{
  "name": "reactulator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "proxy": "http://localhost:3000",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.tsx",
    "build": "cd ui && npm install && npm run build",
    "serves": "cd ui && npm install && npm run build &&  serve -s build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "npm": "11.3.0",
    "node": "22.14.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.6",
    "express": "^5.1.0",
    "react-bootstrap": "^2.10.10"
  }
}
